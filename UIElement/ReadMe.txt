This adddon is for those who wish to spawn 2D UI elements onto a canvas without using an Orthographic
2D camera. This is a limitation in the Unity engine, and this is a workaround to produce the same effect
with the 3D camera. 